<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %>'s message</title>
    <link rel="stylesheet" href="/stylesheets/message.css">
</head>
<body>
    <header class="header">
        <h1><%= headerTitle %></h1>
    </header>
    <h2>Message Detail</h2>
    <div class="details">
        <h2><%=message[0].author %></h2>
        <p><%= message[0].message %> </p>
        <div class="btn-date">
            <span><%= message[0].added.toLocaleDateString() +  " " + " " + " "  + message[0].added.toLocaleTimeString() %></span>
            <button onclick="window.location.href='/'">Back</button>
        </div>
    </div>
</body>
</html>